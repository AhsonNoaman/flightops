# The public npm registry, pinned explicitly.
#
# The machine this was built on defaults to a corporate mirror, and a lockfile resolved against
# it would bake internal hostnames into a public repository and fail to install anywhere else.
registry=https://registry.npmjs.org/
